# Core LLM Framework
langchain
langchain-core

# Agent Framework
langgraph

# OpenAI Compatible Gateway (Allows setting custom base_url for LiteLLM, vLLM, Ollama, etc.)
langchain-openai

# Observability (Standard for LangChain/LangGraph)

# Data validation
pydantic

# Environment variable management (for API keys and gateway URLs)
python-dotenv
# Testing
pytest
pytest-asyncio

# Data Analysis (for local observability logs)
pandas
